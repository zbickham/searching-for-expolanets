---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-test-db-pvc
  labels:
    username: app
    env: test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
